# Hotel Chat Bot — Руководство для владельца

> **Контакт разработчика:** [Имя / телефон / email — заполнить]

Бот автоматически отвечает на сообщения гостей в WhatsApp (и Telegram — в следующей версии). Написан на Python, работает на сервере 24/7.

---

## Что бот делает автоматически
- Отвечает на вопросы о ценах, заезде/выезде, удобствах, как добраться
- Собирает заявки на бронирование (имя, даты, количество гостей)
- Отправляет вам уведомление в WhatsApp, когда гость хочет забронировать
- Отвечает на кыргызском, если гость пишет по-кыргызски
- Отвечает на русском по умолчанию
- Помнит историю переписки с каждым гостем

## Что вы по-прежнему делаете сами
- Подтверждаете бронирование, связываясь с гостем напрямую
- Отправляете реквизиты для оплаты (бот их никогда не раскрывает)
- Отвечаете на вопросы, которые бот не может решить — он напишет "уточню у администратора"

---

## Как обновить информацию об отеле

Все данные отеля хранятся в одном файле — `system-prompt.txt` на сервере.

```bash
# Подключитесь к серверу
ssh hotelbot@ВАШ_IP

# Откройте файл
nano system-prompt.txt

# Замените текст в [СКОБКАХ] на реальные данные:
# [НАЗВАНИЕ ОТЕЛЯ], [ПОЛНЫЙ АДРЕС], [ЦЕНА], [КАК ДОБРАТЬСЯ] и т.д.

# Сохраните: Ctrl+O → Enter → Ctrl+X

# Перезапустите бот
systemctl restart hotel-chat-bot
```

Изменения вступят в силу немедленно — перезапуск занимает несколько секунд.

---

## Если бот перестал отвечать

```bash
# Подключитесь к серверу
ssh hotelbot@ВАШ_IP

# Проверьте статус
systemctl status hotel-chat-bot

# Посмотрите последние ошибки
journalctl -u hotel-chat-bot -n 50

# Перезапустите
systemctl restart hotel-chat-bot
```

Бот запускается автоматически при перезагрузке сервера — вручную вмешиваться обычно не нужно.

---

## Ежемесячные расходы

| Сервис | Стоимость |
|---|---|
| Сервер Hetzner (VPS) | ~€4 (~$4.50) / месяц |
| Домен (опционально) | ~$1 / месяц |
| ИИ — OpenAI GPT-4o mini | ~$0.25–0.50 / месяц |
| WhatsApp Business API | бесплатно (входящие) |
| Telegram Bot API | бесплатно |
| База данных Supabase | бесплатно |
| **Итого** | **~$6–7 / месяц** |

### Расчёт стоимости ИИ (OpenAI GPT-4o mini)

Модель: `gpt-4o-mini` — $0.15 за 1M входящих токенов, $0.60 за 1M исходящих токенов.

| Объём | Входящих токенов | Исходящих токенов | Стоимость |
|---|---|---|---|
| 500 сообщений/мес (низкий сезон) | ~500K | ~75K | ~$0.12 |
| 1 000 сообщений/мес (обычный) | ~1M | ~150K | ~$0.24 |
| 3 000 сообщений/мес (высокий сезон) | ~3M | ~450K | ~$0.72 |

*Расчёт на одно сообщение: ~900 токенов вход (системный промпт ~500 + история ~380 + вопрос ~20) + ~150 токенов выход.*

При любом реалистичном объёме для небольшого отеля стоимость ИИ не превысит **$1/месяц**.

---

## Файлы проекта

| Файл | Назначение |
|---|---|
| `system-prompt.txt` | Данные отеля и инструкции для бота — **редактировать здесь** |
| `test-messages.txt` | Тест-скрипт для проверки бота вручную |
| `docs/meta-setup.md` | Инструкция по подключению WhatsApp Business API |
| `docs/tech-requirements-python.md` | Полная техническая документация |

---

## Структура кода (для разработчика)

```
hotel-chat-bot/
├── app.py                  ← Flask: маршруты вебхуков
├── core/
│   ├── bot.py              ← Логика OpenAI GPT-4o mini + история диалога
│   ├── db.py               ← Supabase: чтение/запись истории
│   └── notify.py           ← Уведомления владельцу
├── platforms/
│   ├── whatsapp.py         ← Обработчик WhatsApp (v1)
│   └── telegram.py         ← Обработчик Telegram (v2)
├── system-prompt.txt       ← Промпт с данными отеля
└── deploy/                 ← Конфиги systemd и nginx
```

Подробнее: [`docs/tech-requirements-python.md`](docs/tech-requirements-python.md)
